apiVersion: v1
kind: ConfigMap
metadata:
  name: lornu-ai-config
  labels:
    app: lornu-ai
data:
  # Base configuration - environment-agnostic defaults
  LOG_LEVEL: "info"
  ENVIRONMENT: "development"  # Safe default; production overlay will override
