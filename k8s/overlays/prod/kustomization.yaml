apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: prod-
namespace: default

# Use production ECR image
images:
  - name: lornu-ai
    newName: <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/lornu-ai-prod
    newTag: latest

# ConfigMap generator for production-specific configuration
configMapGenerator:
  - name: lornu-ai-config
    behavior: merge
    literals:
      - LOG_LEVEL=warn
      - ENVIRONMENT=production
      - DEBUG=false

# Patch for production-specific settings
patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: lornu-ai
      spec:
        replicas: 3
        template:
          spec:
            containers:
            - name: lornu-ai
              resources:
                requests:
                  memory: "512Mi"
                  cpu: "500m"
                limits:
                  memory: "1Gi"
                  cpu: "1000m"
