name: "Terraform AWS"

on:
  pull_request:
    paths:
      - "terraform/aws/staging/**"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  plan:
    name: "Terraform Plan"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Configure AWS Credentials"
        id: aws-creds
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions
          aws-region: us-east-2
          role-session-name: github-actions-terraform-plan

      - name: "Setup Terraform"
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.8.0
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: "Terraform Format Check"
        id: fmt
        run: terraform -chdir=terraform/aws/staging fmt -check

      - name: "Configure Terraform Cloud Backend"
        run: |
          sed -i 's/organization = "lornu-ai"/organization = "${{ secrets.TF_CLOUD_ORG }}"/' terraform/aws/staging/main.tf
          sed -i 's/name = "lornu-ai-staging-aws"/name = "${{ secrets.TF_CLOUD_WORKSPACE }}"/' terraform/aws/staging/main.tf

      - name: "Terraform Init"
        id: init
        continue-on-error: true
        run: terraform -chdir=terraform/aws/staging init

      - name: "Terraform Validate"
        id: validate
        if: steps.init.outcome == 'success'
        run: terraform -chdir=terraform/aws/staging validate

      - name: "Terraform Plan"
        id: plan
        if: steps.aws-creds.outcome == 'success' && steps.init.outcome == 'success' && steps.validate.outcome == 'success'
        run: terraform -chdir=terraform/aws/staging plan -no-color
        continue-on-error: true

      - name: "Comment PR with Plan"
        if: github.event_name == 'pull_request' && steps.plan.outcome != 'skipped'
        continue-on-error: true
        uses: actions/github-script@v7
        env:
          PLAN: ${{ steps.plan.outputs.stdout }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
            #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
            #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
            #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`terraform
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: "Comment PR - Missing Credentials"
        if: github.event_name == 'pull_request' && (steps.aws-creds.outcome == 'failure' || steps.init.outcome == 'failure' || steps.plan.outcome == 'skipped')
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const missing = [];
            if ('${{ steps.aws-creds.outcome }}' === 'failure') {
              missing.push('AWS_ACCOUNT_ID (or OIDC not configured)');
            }
            if ('${{ steps.init.outcome }}' === 'failure') {
              missing.push('TF_API_TOKEN');
            }

            const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
            #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
            ⚠️ **Terraform Plan Skipped**: Missing required configuration: ${missing.join(', ')}

            The Terraform code has been validated and formatted correctly. To run the full plan, configure the following:
            - \`AWS_ACCOUNT_ID\`: AWS account ID for OIDC authentication (and ensure OIDC is configured in AWS)
            - \`TF_API_TOKEN\`: Terraform Cloud API token
            - \`TF_CLOUD_ORG\`: Terraform Cloud organization name
            - \`TF_CLOUD_WORKSPACE\`: Terraform Cloud workspace name

            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: "Terraform Plan Status"
        if: steps.plan.outcome == 'failure' && steps.plan.outcome != 'skipped'
        run: exit 1

      - name: "Terraform Security Scan"
        uses: aquasecurity/tfsec-action@v1.0.0
        with:
          working_directory: terraform/aws/staging

  apply:
    name: "Terraform Apply"
    runs-on: ubuntu-latest
    needs: plan
    if: github.event_name == 'workflow_dispatch'

    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Configure AWS Credentials"
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions
          aws-region: us-east-2
          role-session-name: github-actions-terraform-apply

      - name: "Setup Terraform"
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.8.0
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: "Configure Terraform Cloud Backend"
        run: |
          sed -i 's/organization = "lornu-ai"/organization = "${{ secrets.TF_CLOUD_ORG }}"/' terraform/aws/staging/main.tf
          sed -i 's/name = "lornu-ai-staging-aws"/name = "${{ secrets.TF_CLOUD_WORKSPACE }}"/' terraform/aws/staging/main.tf

      - name: "Terraform Init"
        run: terraform -chdir=terraform/aws/staging init

      - name: "Terraform Apply"
        run: terraform -chdir=terraform/aws/staging apply -auto-approve
