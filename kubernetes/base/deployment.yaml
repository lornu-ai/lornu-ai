apiVersion: apps/v1
kind: Deployment
metadata:
  name: lornu-ai
  labels:
    app.kubernetes.io/name: lornu-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: lornu-ai
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lornu-ai
    spec:
      serviceAccountName: lornu-ai
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: lornu-ai
        image: lornu-ai:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: PORT
          value: "8080"
        - name: HOST
          value: "0.0.0.0"
        - name: PYTHONPATH
          value: "/app"
        - name: AWS_REGION
          value: "us-east-1"
        - name: S3_ASSETS_BUCKET
          value: "lornu-ai-assets-${AWS_ACCOUNT_ID}"
        - name: DYNAMODB_RATE_LIMIT_TABLE
          value: "lornu-ai-rate-limit-kv"
        - name: DYNAMODB_GENERAL_KV_TABLE
          value: "lornu-ai-general-kv"
        envFrom:
        - configMapRef:
            name: lornu-ai-config
        - secretRef:
            name: lornu-ai-secrets
            optional: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
