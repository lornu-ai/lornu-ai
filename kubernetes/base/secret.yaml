apiVersion: v1
kind: Secret
metadata:
  name: lornu-ai-secrets
type: Opaque
stringData:
  # RESEND_API_KEY should be set via kubectl or external secrets
  # Example: kubectl create secret generic lornu-ai-secrets --from-literal=RESEND_API_KEY=re_xxx -n lornu-dev
  RESEND_API_KEY: ""
