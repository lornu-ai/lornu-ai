apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# cert-manager native K8s manifests (no Helm, pure manifests)
resources:
- namespace-rbac.yaml
- deployment.yaml
- service.yaml
- clusterissuer.yaml

commonLabels:
  app.kubernetes.io/part-of: lornu-ai
  lornu.ai/component: tls-management

namespace: cert-manager
