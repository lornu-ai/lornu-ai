ignore:
  - id: aws-eks-no-public-cluster-access
    reason: "GitHub Actions requires public endpoint to obtain auth token via OIDC and AWS CLI for cluster provisioning. Access restricted via security controls."
    expiry: "2026-12-31"
  - id: aws-eks-no-public-cluster-access-to-cidr
    reason: "Public access CIDR 0.0.0.0/0 needed for CI workflows during cluster bootstrap. Additional network policies and authentication enforce access."
    expiry: "2026-12-31"
  - id: aws-ec2-no-public-egress-sgr
    reason: "Node group egress is required for pulling images and contacting AWS APIs. Controlled via IAM roles and security policies."
    expiry: "2026-12-31"
  - id: aws-iam-no-policy-wildcards
    reason: "Temporary wildcard IAM actions used for CI automation (Terraform, controller setup). Will be tightened after baseline deployment."
    expiry: "2026-06-30"